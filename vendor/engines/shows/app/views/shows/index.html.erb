<% content_for :body_content_left do %>
  <ul id="shows">
    <% @shows.each do |show| %>
      <li>
        <%= link_to show.name, show_path(show) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
